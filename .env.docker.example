# Устарело как отдельный файл.
# Единый источник секретов: backend/.env (шаблон: backend/.env.example)
#
#   cp backend/.env.example backend/.env
#   # заполните SECRET_KEY, AGENT_TOKEN, AGENT_TOKEN_PEPPER,
#   # BOOTSTRAP_ADMIN_PASSWORD, POSTGRES_PASSWORD
#   npm run docker:up

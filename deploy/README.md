# deploy/

Production CORAX — **только Docker Compose**.

Первый запуск: [GETTING_STARTED.md](../GETTING_STARTED.md). Ops: [docs/docker.md](../docs/docker.md).

- `docker/entrypoint.sh` — вход контейнера `app`
- `docker/backup-now.sh` — ручной дамп sidecar
